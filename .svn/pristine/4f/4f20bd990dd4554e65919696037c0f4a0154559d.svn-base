document.write('<script src="/static/scripts/gecko-modules/jquery/jquery-2.2.1.min.js"></script>');

    function showProduct(){
    	//alert(11);
    	$("#isProduct").show();
    }
    function hideProduct(){
    	$("#isProduct").hide();
    }
    function checkContent(form, evt){
    	
    	$("#registrationarea").val($("#qixian1").val());
    	
    	/**    	 * 验证必填字段
    	 */
    	if($("#operator").val().length==0){
    		alert("请填入登记人姓名！");
    		if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}else if($("#operator").val().length>10){
    		alert("登记人姓名过长！");
    		if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}
    	if($("#businessType").val()==0){
    		alert("请选择业务类型！");
    		if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}
    	if($("#source").val()==0){
    		alert("请选择业务来源！");
    		if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}
    	if($("#acceptype").val()==0){
    		alert("请选择接收方式！");
    		if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}
    	//不是匿名
    	//alert($("input[name='isanonymous']:checked").val());
    	if($("input[name='isanonymous']:checked").val()==1){
    		if($("#complainantname").val().length==0){
        		alert("请填入投诉举报人姓名！");
        		　if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}else if($("#complainantname").val().length>10){
        		alert("投诉举报人姓名过长！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#tel").val().length==0){
        		alert("请填入投诉举报人联系电话!");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}else{
        		 var regTel1 = /^(([0\+]\d{2,3}-)?(0\d{2,3})-)(\d{7,8})(-(\d{3,}))?$/.test($("#tel").val());//带区号的固定电话
        	     var regTel2 = /^(\d{7,8})(-(\d{3,}))?$/.test($("#tel").val());//不带区号的固定电话
        	     var regTel3 = /^1[34578]\d{9}$/.test($("#tel").val());
        	     
        	     if(!(regTel1 || regTel2 || regTel3)){
        	    	 alert("投诉举报人联系电话格式不正确!");
        	    	 $("#tel").closest('.form-group').removeClass('has-info').addClass('has-error');
            		　if (evt && evt.preventDefault) {
            			evt.preventDefault();
            		}else{
            			window.event.returnValue = false;//如果是IE下执行这个
            		}
        	     }else{
        	    	 $("#tel").closest('.form-group').removeClass('has-info').removeClass('has-error'); 
        	     }
        	}
    		
    		if($("#address").val().length==0){
        		alert("请填入投诉举报人联系地址！");
        		　if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#licensetype").val()==0){
        		alert("请选择投诉举报人的证件类型！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#complainantlicense").val().length==0){
        		alert("请填入投诉举报人证件号码！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}else{
        		if($("#licensetype").val()==1){
       			 var regLicense = /(^\d{15}$)|(^\d{18}$)|(^\d{17}(\d|X|x)$)/.test($("#complainantlicense").val());
       			 if(!regLicense){
       				 alert("投诉举报人身份证号格式不正确!");
           	    	 $("#complainantlicense").closest('.form-group').removeClass('has-info').addClass('has-error');
           	    	if (evt && evt.preventDefault) {
            			evt.preventDefault();
            		}else{
            			window.event.returnValue = false;//如果是IE下执行这个
            		}
           	     }else{
           	    	 $("#complainantlicense").closest('.form-group').removeClass('has-info').removeClass('has-error'); 
           	     }
       		}
        	}
    	}
    	
    	if($("#respondentname").val().length==0){
    		alert("请填入被投诉人名称！");
    		if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}else if($("#respondentname").val().length>10){
    		alert("被投诉人名称过长！");
    		if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}
    	if($("#respondenttel").val().length==0){
    		alert("请填入被投诉人联系电话！");
    		if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}else{
   		 var regTel1 = /^(([0\+]\d{2,3}-)?(0\d{2,3})-)(\d{7,8})(-(\d{3,}))?$/.test($("#respondenttel").val());//带区号的固定电话
	     var regTel2 = /^(\d{7,8})(-(\d{3,}))?$/.test($("#respondenttel").val());//不带区号的固定电话
	     var regTel3 = /^1[34578]\d{9}$/.test($("#respondenttel").val());
	     
	     if(!(regTel1 || regTel2 || regTel3)){
	    	 alert("被投诉举报人联系电话格式不正确!");
	    	 $("#respondenttel").closest('.form-group').removeClass('has-info').addClass('has-error');
    		　if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
	     }else{
	    	 $("#respondenttel").closest('.form-group').removeClass('has-info').removeClass('has-error'); 
	     }
	}
    	if($("#respondentaddress").val().length==0){
    		alert("请填入被投诉人联系地址！");
    		if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}
    	//alert($("input[name='isProductRadio']:checked").val());
    	//涉及产品
    	if($("input[name='isProductRadio']:checked").val()==0){
    		if($("#product1").val().length==0){
        		alert("请填入涉及产品名称！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#producttype").val()==0){
        		alert("请选择涉及产品类型!");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#approvalNumber").val().length==0){
        		alert("请填入产品批准文号！");
        		　if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#spec").val().length==0){
        		alert("请填入产品型号规格！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#batchNumber").val().length==0){
        		alert("请填入产品批号！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#manufacturer").val().length==0){
        		alert("请填入产品生产企业！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#temp1").val().length==0){
        		alert("请填入产品销售企业！");
        		　evt.preventDefault();
        	}
    		if($("#dateOfManufacture").val().length==0){
        		alert("请填入产品生产日期！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#validitydate").val().length==0){
        		alert("请填入产品有效期！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    	}
    	
    	if($("#title").val().length==0){
    			alert("请填入标题！");
    		　if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}
    	if($("#responsecontent").val().length==0){
			alert("请填入受理信息的主要内容！");
		　if (evt && evt.preventDefault) {
			evt.preventDefault();
		}else{
			window.event.returnValue = false;//如果是IE下执行这个
		}
    	}
    	if($("#opinion").val().length==0){
			alert("请填入受理信息的处理意见！");
			if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	}
    	
    	if($("#operation").val()==0){
    		alert("请选择业务办理类型!");
    		if (evt && evt.preventDefault) {
    			evt.preventDefault();
    		}else{
    			window.event.returnValue = false;//如果是IE下执行这个
    		}
    	
    	//交办
    	if($("#operation").val()==1){
    		if($("#nextAssignId").val().length==0){
        		alert("请选择拟交办单位！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#commit").val().length==0){
        		alert("请填写拟交办意见！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    	}
    	//送审
    	if($("#operation").val()==2){
    		if($("#nextAssignId").val().length==0){
        		alert("请选择拟交办单位！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#nextAssignIdMengshi").val().length==0){
        		alert("请选择盟市办理单位！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    		if($("#commit").val().length==0){
        		alert("请填写拟交办意见！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    	}
    	//办结和同意受理
    	if($("#operation").val()==3 || $("#operation").val()==4){
    		if($("#commit").val().length==0){
        		alert("请填写拟交办意见！");
        		if (evt && evt.preventDefault) {
        			evt.preventDefault();
        		}else{
        			window.event.returnValue = false;//如果是IE下执行这个
        		}
        	}
    	}
    }
    }
    function adjustment(form){
    	
    	/**
    	 * 验证必填字段
    	 */
    	if($("#operator").val().length==0){
    		alert("请填入登记人姓名！");
    		return false;
    	}else if($("#operator").val().length>10){
    		alert("登记人姓名过长！");
    		return false;
    	}
    	if($("#businessType").val()==0){
    		alert("请选择业务类型！");
    		return false;
    	}
    	if($("#source").val()==0){
    		alert("请选择业务来源！");
    		return false;
    	}
    	if($("#acceptype").val()==0){
    		alert("请选择接收方式！");
    		return false;
    	}
    	//不是匿名
    	//alert($("input[name='isanonymous']:checked").val());
    	if($("input[name='isanonymous']:checked").val()==1){
    		if($("#complainantname").val().length==0){
        		alert("请填入投诉举报人姓名！");
        		return false;
        	}else if($("#complainantname").val().length>10){
        		alert("投诉举报人姓名过长！");
        		return false;
        	}
    		if($("#tel").val().length==0){
        		alert("请填入投诉举报人联系电话!");
        		return false;
        	}else{
        		 var regTel1 = /^(([0\+]\d{2,3}-)?(0\d{2,3})-)(\d{7,8})(-(\d{3,}))?$/.test($("#tel").val());//带区号的固定电话
        	     var regTel2 = /^(\d{7,8})(-(\d{3,}))?$/.test($("#tel").val());//不带区号的固定电话
        	     var regTel3 = /^1[34578]\d{9}$/.test($("#tel").val());
        	     
        	     if(!(regTel1 || regTel2 || regTel3)){
        	    	 alert("投诉举报人联系电话格式不正确!");
        	    	 $("#tel").closest('.form-group').removeClass('has-info').addClass('has-error');
            		　return false;
        	     }else{
        	    	 $("#tel").closest('.form-group').removeClass('has-info').removeClass('has-error'); 
        	     }
        	}
    		
    		if($("#address").val().length==0){
        		alert("请填入投诉举报人联系地址！");
        		　return false;
        	}
    		if($("#licensetype").val()==0){
    			return false;
        	}
    		if($("#complainantlicense").val().length==0){
        		alert("请填入投诉举报人证件号码！");
        		return false;
        	}else{
        		if($("#licensetype").val()==1){
       			 var regLicense = /(^\d{15}$)|(^\d{18}$)|(^\d{17}(\d|X|x)$)/.test($("#complainantlicense").val());
       			 if(!regLicense){
       				 alert("投诉举报人身份证号格式不正确!");
           	    	 $("#complainantlicense").closest('.form-group').removeClass('has-info').addClass('has-error');
           	    	return false;
           	     }else{
           	    	 $("#complainantlicense").closest('.form-group').removeClass('has-info').removeClass('has-error'); 
           	     }
       		}
        	}
    	}
    	
    	if($("#respondentname").val().length==0){
    		alert("请填入被投诉人名称！");
    		return false;
    	}else if($("#respondentname").val().length>10){
    		alert("被投诉人名称过长！");
    		return false;
    	}
    	if($("#respondenttel").val().length==0){
    		alert("请填入被投诉人联系电话！");
    		return false;
    	}else{
   		 var regTel1 = /^(([0\+]\d{2,3}-)?(0\d{2,3})-)(\d{7,8})(-(\d{3,}))?$/.test($("#respondenttel").val());//带区号的固定电话
	     var regTel2 = /^(\d{7,8})(-(\d{3,}))?$/.test($("#respondenttel").val());//不带区号的固定电话
	     var regTel3 = /^1[34578]\d{9}$/.test($("#respondenttel").val());
	     
	     if(!(regTel1 || regTel2 || regTel3)){
	    	 alert("被投诉举报人联系电话格式不正确!");
	    	 $("#respondenttel").closest('.form-group').removeClass('has-info').addClass('has-error');
    		　return false;
	     }else{
	    	 $("#respondenttel").closest('.form-group').removeClass('has-info').removeClass('has-error'); 
	     }
	}
    	if($("#respondentaddress").val().length==0){
    		alert("请填入被投诉人联系地址！");
    		return false;
    	}
    	//alert($("input[name='isProductRadio']:checked").val());
    	//涉及产品
    	if($("input[name='isProductRadio']:checked").val()==0){
    		
    		if($("#product1").val().length==0){
        		alert("请填入涉及产品名称！");
        		return false;
        	}
    		if($("#producttype").val()==0){
        		alert("请选择涉及产品类型!");
        		return false;
        	}
    		if($("#approvalNumber").val().length==0){
        		alert("请填入产品批准文号！");
        		return false;
        	}
    		if($("#spec").val().length==0){
        		alert("请填入产品型号规格！");
        		return false;
        	}
    		if($("#batchNumber").val().length==0){
        		alert("请填入产品批号！");
        		return false;
        	}
    		if($("#manufacturer").val().length==0){
        		alert("请填入产品生产企业！");
        		return false;
        	}
    		if($("#temp1").val().length==0){
        		alert("请填入产品销售企业！");
        		　return false;
        	}
    		if($("#dateOfManufacture").val().length==0){
        		alert("请填入产品生产日期！");
        		return false;
    		}
        	if($("#validitydate").val().length==0){
        		alert("请填入产品有效期！");
        		return false;
        	}
    	}
    	
    	if($("#title").val().length==0){
    			alert("请填入标题！");
    		　return false;
    	}
    	if($("#responsecontent").val().length==0){
			alert("请填入受理信息的主要内容！");
			return false;
    	}
    	if($("#opinion").val().length==0){
			alert("请填入受理信息的处理意见！");
			return false;
    	}
    	
    	$("#registrationarea").val($("#qixian1").val());   	
    	$( "#showRepeat" ).dialog({      
	 		height: 550,
      		width:680,
      		modal: true
		});
	var tableId = "repeatTable";
	$("#"+ tableId).bootstrapTable('destroy'); 
	$("#" + tableId).bootstrapTable({
	 			method: 'post',
	 			contentType:"application/x-www-form-urlencoded; charset=UTF-8", 
	 			queryParams:queryParams_3,
				editable:false,//关闭编辑模式  
                toolbar: "#toolbar",
                pagination: true,
                //showRefresh: true,
                sidePagination: "client",
               // pageNumber:1,   
              	pageSize: 5,
                url: "/complaint/cpcomplaintsreport/adjustment",
                columns: [{
                    checkbox: true
                }, {
                    field: "id",
                    visible: false
                }, {
                    field: "cpComplaintsreportId",
                    title: "信息编号",
                    type: "text",
                    search: "true",
                    editable: {
                        type: "text"
                    }
                }, {
                    field: "source",
                    title: "业务来源",
                    type: "text",
                    search: "true",
                    editable: {
                        type: "text"
                    }
                },{
                    field: "businessType",
                    title: "业务类型",
                    type: "text",
                    search: "true",
                    editable: {
                        type: "text"
                    }
                },{
                    field: "registerdate",
                    title: "登记时间",
                    type: "text",
                    search: "true",
                    editable: {
                        type: "text"
                    }
                },{
                title: '查看详情',
                align: 'center',
                width : 100,
                events: operateEvents_3,
                formatter: operateFormatter3
                }]
            });
    }
    

 	